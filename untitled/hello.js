const num1 = 1;
const num2 = 2;
let result = num1 + num2;

const string1 = num1 + "과" + num2 + "더한 결과는 " + num1 + num2 + "이다";
console.log(string1);
const string2 = `${num1}과 ${num2} 더한 결과는 ${num1+num2}이다`;
console.log(string2);
